import * as React from 'react';

const NoPage = () => (
  <div>
    <h1>No Page</h1>
  </div>
)
function App() {
  const [page, setPage] = React.useState(false);
  
  return (
    <div>
     
    </div>
  )
}

export default App